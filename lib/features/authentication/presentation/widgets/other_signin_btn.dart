import 'package:flutter/material.dart';

class OtherButtons extends StatelessWidget {
  const OtherButtons({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
